---
- name: Create the {{container_name}} config folder
  ansible.builtin.file:
    path: "{{ docker_dir }}/{{container_name}}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0755

- name: Make sure the {{container_name}} container is created and running
  community.general.docker_container:
    name: "{{container_name}}"
    image: homeassistant/home-assistant:2026.3.1
    pull: yes
    state: "started"
    privileged: true
    network_mode: "host"
    volumes:
      - "{{ docker_dir }}/{{container_name}}/config:/config"
      - "/etc/localtime:/etc/localtime:ro"
      - "{{ docker_dir }}/{{container_name}}/media:/media"
    restart_policy: always
  become: false

- name: Create the matter-server data folder
  ansible.builtin.file:
    path: "{{ docker_dir }}/matter-server"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0755

- name: Add IPv6 Thread network routes via HomePods
  command: ip -6 route replace fd06:7a9e:fd01::/64 via {{ item }} dev eth0
  loop:
    - fe80::c83:3dec:6ec:29a7
    - fe80::44e:2860:220c:7987
    - fe80::14bb:c8f8:7483:4184
    - fe80::1c89:44e7:b13b:3cb9
    - fe80::1015:e3f:1c19:a025
    - fe80::1ca5:f825:9569:23d9
    - fe80::d1:7ec0:8db6:1de7
  become: true

- name: Accept IPv6 RAs on eth0 even with forwarding enabled
  sysctl:
    name: net.ipv6.conf.eth0.accept_ra
    value: '2'
    sysctl_file: /etc/sysctl.d/99-ipv6-ra.conf
    reload: yes
  become: true

- name: Make sure the matter-server container is created and running
  community.general.docker_container:
    name: matter-server
    image: ghcr.io/home-assistant-libs/python-matter-server:8.1
    pull: yes
    state: started
    network_mode: host
    command: ["--storage-path", "/data", "--primary-interface", "eth0"]
    volumes:
      - "{{ docker_dir }}/matter-server:/data"
      - "/run/dbus:/run/dbus"
    restart_policy: always
  become: false

- name: Make {{container_name}} backup directory
  ansible.builtin.file:
    path: "{{ backups_dir }}/{{container_name}}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0755

- name: Copy backup script file in place.
  template:
    src: "templates/backup-basic-docker.sh.j2"
    dest: "/usr/local/bin/backup-{{container_name}}.sh"
    owner: root
    group: root
    mode: 0755

- name: Add cron job to run backup weekly, at midnight on Sundays.
  ansible.builtin.cron:
    name: Run {{container_name}}-backup script.
    minute: "0"
    hour: "0"
    weekday: "0"
    job: "/usr/local/bin/backup-{{container_name}}.sh"
    state: present
  become: false

- name: Make matter-server backup directory
  ansible.builtin.file:
    path: "{{ backups_dir }}/matter-server"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0755

- name: Copy matter-server backup script file in place.
  template:
    src: "templates/backup-basic-docker.sh.j2"
    dest: "/usr/local/bin/backup-matter-server.sh"
    owner: root
    group: root
    mode: 0755
  vars:
    container_name: matter-server

- name: Add cron job to run matter-server backup weekly, at midnight on Sundays.
  ansible.builtin.cron:
    name: Run matter-server-backup script.
    minute: "0"
    hour: "0"
    weekday: "0"
    job: "/usr/local/bin/backup-matter-server.sh"
    state: present
  become: false