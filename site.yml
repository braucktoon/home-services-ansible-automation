---
- name: base build
  hosts: [service_hosts]
  become: true

  roles:
      - role: security
        tags: [ security ]
      - role: nut-client
        tags: [ nut-client ]
      - role: docker
        tags: [ docker ]

- name: build nut proxy service
  hosts: [nut-proxy]
  handlers:
  - import_tasks: roles/nut-client/handlers/main.yml
  become: true

  roles:
      - role: security
        tags: [ security ]
      - role: nut-client
        tags: [ nut-client ]
      - role: nginx-nut-proxy
        tags: [ nginx-nut-proxy ]
