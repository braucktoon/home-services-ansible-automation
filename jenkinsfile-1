pipeline {
  agent { dockerfile true }
  environment {
    VAULT_PW = credentials('vaultpw')
  }
  stages {
    stage('Test') {
      steps {
        script {
            env.DEFAULT_LOCAL_TMP = env.WORKSPACE_TMP
            env.HOME = env.WORKSPACE

            sh """
            ping 10.0.0.2
            """

            sh """
            ansible-playbook -i inventory --vault-password-file "$VAULT_PW" site.yml --tags jenkins
            """
        }
      }
    }
  }
}