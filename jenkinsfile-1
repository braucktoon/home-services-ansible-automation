pipeline {
  agent { dockerfile true }
  environment {
    VAULT_PW = credentials('vaultpw')
  }
  stages {
    stage('Test') {
      steps {
        echo "My ansbile vault pw is ${VAULT_PW}"
      }
    }
  }
}