pipeline {
  agent { dockerfile true }
  environment {
    VAULT_PW = credentials('vaultpw')
  }
  stages {
    stage('Test') {
      steps {
        sh('ls -al')
        sh('ansible')
      }
    }
  }
}