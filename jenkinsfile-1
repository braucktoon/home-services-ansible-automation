pipeline {
  agent { dockerfile true }
  environment {
    VAULT_PW = credentials('vaultpw')
  }
  stages {
    stage('Test') {
      steps {
        sh('env.DEFAULT_LOCAL_TMP = env.WORKSPACE_TMP')
        sh('env.HOME = env.WORKSPACE')
        sh('ls -al')
        sh('ansible')
      }
    }
  }
}